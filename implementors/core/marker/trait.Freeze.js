(function() {var implementors = {};
implementors["dir_assert"] = [{text:"impl Freeze for <a class=\"enum\" href=\"dir_assert/enum.Error.html\" title=\"enum dir_assert::Error\">Error</a>",synthetic:true,types:["dir_assert::error::Error"]},];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        })()